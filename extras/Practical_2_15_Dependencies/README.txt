Optional dependency ZIPs supplied with the original practical.
If Library Manager cannot locate a compatible version, install these ZIPs
using Sketch -> Include Library -> Add .ZIP Library.

Recommended: use Library Manager first, because it is easier to maintain
and update dependencies.
